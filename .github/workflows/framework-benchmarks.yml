name: Framework benchmarks

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'docs/**'
  schedule:
    - cron: '17 2 1 * *'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: framework-benchmarks
  cancel-in-progress: true

jobs:
  benchmark:
    name: Benchmark and propose reports
    runs-on: ubuntu-latest
    timeout-minutes: 330

    steps:
      - name: Check out main
        uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: curl, intl, mbstring, pdo_sqlite, sqlite3, zip
          coverage: none
          tools: composer:v2

      - name: Install benchmark dependencies
        run: composer install --no-interaction --prefer-dist --no-progress

      - name: Generate latest OPcache framework applications
        run: composer benchmark:frameworks -- setup --runtime=opcache --force

      - name: Generate latest Swoole framework applications
        run: composer benchmark:frameworks -- setup --runtime=swoole --force

      - name: Start OPcache benchmark server
        run: composer benchmark:frameworks -- docker --runtime=opcache

      - name: Validate OPcache framework targets
        run: composer benchmark:frameworks -- check --runtime=opcache

      - name: Run complete OPcache benchmark suite
        run: composer benchmark:frameworks -- run --runtime=opcache --output=docs/opcache --stability=10 --history-limit=61 --replace-monthly

      - name: Show OPcache benchmark server errors
        if: failure()
        run: docker logs --tail 200 benchmark-frameworks-apache || true

      - name: Stop OPcache benchmark server
        if: always()
        run: |
          if [[ -f frameworks/.benchmark-server.json ]]; then
            composer benchmark:frameworks -- docker-stop
          fi

      - name: Start Swoole benchmark server
        run: composer benchmark:frameworks -- docker --runtime=swoole

      - name: Validate Swoole framework targets
        run: composer benchmark:frameworks -- check --runtime=swoole

      - name: Run complete Swoole benchmark suite
        run: composer benchmark:frameworks -- run --runtime=swoole --output=docs/swoole --stability=10 --history-limit=61 --replace-monthly

      - name: Show Swoole benchmark server errors
        if: failure()
        run: docker logs --tail 200 benchmark-frameworks-swoole || true

      - name: Stop Swoole benchmark server
        if: always()
        run: |
          if [[ -f frameworks/.benchmark-server.json ]]; then
            composer benchmark:frameworks -- docker-stop
          fi

      - name: Open or update benchmark results pull request
        id: results-pr
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          base: main
          branch: automation/framework-benchmark-results
          delete-branch: true
          add-paths: docs
          commit-message: 'docs: update framework benchmark reports'
          title: 'docs: update framework benchmark reports'
          body: |
            Automated PHP framework benchmark results.

            Generated by workflow run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          author: 'github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>'
          committer: 'github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>'

      - name: Add pull request to workflow summary
        if: steps.results-pr.outputs.pull-request-number != ''
        env:
          RESULTS_PR_NUMBER: ${{ steps.results-pr.outputs.pull-request-number }}
          RESULTS_PR_URL: ${{ steps.results-pr.outputs.pull-request-url }}
          RESULTS_PR_OPERATION: ${{ steps.results-pr.outputs.pull-request-operation }}
        run: |
          echo "### Benchmark results pull request" >> "$GITHUB_STEP_SUMMARY"
          echo "PR [#${RESULTS_PR_NUMBER}](${RESULTS_PR_URL}) was ${RESULTS_PR_OPERATION}." >> "$GITHUB_STEP_SUMMARY"
